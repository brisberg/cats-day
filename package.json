{
  "name": "cats-day",
  "version": "0.1.0",
  "description": "A quirky retelling of a house cat's day.",
  "scripts": {
    "build": "yarn run spindle",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brandon Risberg",
  "license": "MIT",
  "devDependencies": {
    "@brisberg/spindle": "^0.2.1"
  }
}
